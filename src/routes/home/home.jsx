import './home.styles.scss';
import Hero from '../../component/hero/hero.component';

const Home = () => {
    return (
        <main>
            <Hero />
        </main>
    );
};

export default Home;
